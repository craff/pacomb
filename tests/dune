(executable
  (name test)
  (modules test)
  (libraries unix grammar))

(executable
  (name calc)
  (modules calc)
  (flags -ppx "ppx/parserPpx.exe")
  (libraries unix grammar))

(executable
  (name calc_prio)
  (modules calc_prio)
  (flags -ppx "ppx/parserPpx.exe")
  (libraries unix grammar))

(executable
  (name big_expr)
  (modules big_expr)
  (libraries))

(executable
  (name hard)
  (modules hard)
  (flags -ppx "ppx/parserPpx.exe")
  (libraries unix grammar))
