(executable
  (name test)
  (modules test)
  (libraries unix grammar))

(executable
  (name calc)
  (modules calc)
  (libraries unix grammar))

(executable
  (name big_expr)
  (modules big_expr)
  (libraries))
