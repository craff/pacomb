(test
  (name test)
  (modules test)
  (libraries unix grammar)
  (action (run ./test.exe)))

(test
  (name calc)
  (modules calc)
  (libraries unix grammar)
  (action (run -- ./big_expr.exe 4 4 4 | ./calc.exe )))

(executable
  (name big_expr)
  (modules big_expr)
  (libraries))
