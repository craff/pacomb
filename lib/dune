(library
  (name pacomb)
  (public_name pacomb)
  (modules :standard)
  (preprocess (pps pacomb.ppx))
  (libraries stdlib-shims unix)
  (private_modules container hashtbl_eq assoc comb unionFind heap interpolate))

(documentation
  (package pacomb))
